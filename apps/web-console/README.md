# Web Console

Dependency-free localhost workbench for reviewing posts, editing Markdown drafts, publishing, and inspecting snapshots.

Run through the local service:

```bash
TYPECHO_MCP_HOST=your-ssh-alias node apps/local-service/src/server.js
```

Then open `http://127.0.0.1:4783`.

