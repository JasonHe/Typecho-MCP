# Editor

Markdown editor domain helpers will live here.

The first UI milestone should use CodeMirror 6 or an equivalent lightweight Markdown editor surface, but this package is intentionally empty until the desktop app starts.

