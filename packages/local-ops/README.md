# Local Ops

High-level local operations shared by the CLI, web console, and MCP server.

This package owns policy-adjacent behavior such as snapshots before writes. The remote agent remains a small executor.

