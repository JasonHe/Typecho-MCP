# SSH Connector

Small, dependency-free SSH/SCP helpers used by the prototype.

This package intentionally wraps the system `ssh` and `scp` commands for the first milestone. A later implementation can add a native SSH library while keeping the same boundary.

