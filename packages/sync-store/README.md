# Sync Store

Local persistence package for site profiles, drafts, snapshots, operation logs, and MCP invocation history.

SQLite should be the default storage engine once the implementation starts.

